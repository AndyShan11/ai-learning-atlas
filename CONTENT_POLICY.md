# 内容维护与取舍

## 2026-09-12 重构

目标是让零基础读者知道：知识在哪里、同层有哪些、什么时候选择、哪些组件能配合。

| 处理 | 内容 | 原因 |
| --- | --- | --- |
| 重组 | 分离学习方式、模型、训练与后处理 | 避免把深度学习和强化学习错误列为互斥类别 |
| 增补 | LoRA / QLoRA、DPO、GRPO、TabPFN | 加入现代微调、后训练、表格基础模型的入口与边界 |
| 增补 | RAG、嵌入、多模态、生成系统评估 | 补全预训练模型应用链路；并非声称所有方法都发明于 2022 年后 |
| 保留 | SQL、统计、实验、线性模型、树模型、TF-IDF | 低成本基线与分析基础仍有价值 |
| 按需 | RNN / LSTM、SVM | 有具体适用场景，但不作为所有任务的默认路线 |
| 后移 | 大数据平台、分布式工具清单 | 对当前学习目标先理解单机数据工作流更合适；并非行业淘汰声明 |
| 归位 | 学习率调度、校准、阈值、融合 | 明确在训练流程中的位置及与模型的关系 |

## 核查依据

新方法与关键关系参考 PEFT、TRL 的 DPO / GRPO 文档、Prior Labs TabPFN、Stable-Baselines3、PyTorch 调度文档、温度缩放论文、CQL / IQL 论文等。各节点提供对应来源。

对经典基础的内容进行教学归纳，不把官方指南中的示例结果扩写为普遍性能保证。RAG 概念参考 2020 原论文，温度缩放参考 2017 原论文；这些不是 2026 新发明。

## 更新规则

1. 新增节点必须有定义、条件、边界、例子、来源和稳定 id。
2. 新方法不得仅因热度进入“先学”。说明它解决了什么额外问题。
3. 宣称弃用必须有明确维护方证据；用途变窄只标为按需，不直接删除。
4. 区分“包含”“可组合”“对照”“学习顺序”。关联链接统一称为关联，不把所有关系误标为组合。
5. 变更后运行内容检查、生成文字版；界面改动需浏览器验证。
6. 修改 `reviewed` 日期需实际复核内容，不只改年份。版本限制引用维护方资料，避免写死未经核查的上限。

## 已知边界

- 这是知识地图与条件入口，不是最优模型搜索器。
- 不穷尽全部优化、图学习、强化学习或生成模型算法。
- 本图使用单一导航位置加关联链接表达多维关系，导航位置不等于唯一学术分类。
- 选择器提供候选方法，不自动评估数据或运行训练。
- 导出 SVG 包含当前模块结构；完整使用条件在交互详情与文字版中。
